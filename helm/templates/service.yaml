apiVersion: v1
kind: Service
metadata:
  name: {{ include "tp-cicd.fullname" . }}
  labels:
    {{- include "tp-cicd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
  selector:
    {{- include "tp-cicd.selectorLabels" . | nindent 4 }}
